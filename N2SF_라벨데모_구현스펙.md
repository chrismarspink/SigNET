# Signet — N2SF 문서 라벨 데모 · 단일 웹페이지 구현 스펙

> **제품명**: Signet (시그넷) — 문서를 봉인하던 인장 반지에서. 모듈 명칭: Signet Label(발급·서명) / Signet Gate(관문 검증) / Signet Ledger(레지스트리) / Signet Federation(기관 간 상호 인정).
> **목적**: 평가위원·보안성 검토자에게 N2SF 문서 라벨 체계의 두 원리 — **티켓(조직 내 게이트 통제)** 과 **여권(조직 간 상호 인정)** — 을 5~10분 안에 시각적으로 이해시키는 시연용 프로토타입.
> **성격**: 보여주기 전용 데모. 제품 아님. 서버 없음. 설치 없음.
> **산출물**: `index.html` 단 하나의 파일 (외부 라이브러리 0개, CDN 0개, 빌드 0개).

---

## 0. 절대 원칙 (구현 전 숙지)

1. **단일 HTML 파일**: HTML + CSS + JS를 하나의 `index.html`에 전부 포함한다. import, CDN, npm, 빌드 도구 일절 금지. 브라우저에서 파일을 열면 즉시 동작해야 한다.
2. **암호 연산은 진짜여야 한다**: 해시(SHA-256), 서명·검증(ECDSA P-256), 인증서 체인 검증은 전부 **WebCrypto API(`crypto.subtle`) 실연산**으로 구현한다. 판정 결과를 하드코딩하거나 연출하는 것 금지. 위조 시연의 설득력은 "진짜로 검증해서 진짜로 실패하는" 데서 나온다.
3. **파일은 실제 바이트로 유지한다**: 가상 파일이지만 각 파일의 콘텐츠와 라벨은 실제 `Uint8Array`로 존재해야 하며, 해시는 이 바이트에서 실제 계산한다. 사용자가 내용을 1바이트 바꾸면 해시가 실제로 달라져 검증이 실제로 실패해야 한다.
4. **모든 UI 텍스트는 한국어**. 대상 청중은 정부 과제 평가위원이다.
5. **라벨의 가시성이 최우선**: 어떤 파일에 라벨이 있는지/없는지/위조됐는지가 화면에서 3초 안에 구별되어야 한다.
6. 브라우저 대상: 최신 Chrome/Edge. `file://`로 열어도 동작해야 한다(crypto.subtle은 file://에서 secure context로 동작). 만약 동작하지 않는 환경 대비로 페이지 하단에 "동작하지 않으면 `python -m http.server` 후 localhost로 접속" 안내 각주 1줄.

---

## 1. 개념 배경 (해설 패널에 들어갈 내용의 원천)

이 데모가 증명하려는 명제들. 화면의 해설 문구는 이 내용을 압축해 사용한다.

- **라벨은 스스로 내용을 보호하지 않는다.** 라벨은 게이트(CDS·RBI·AI Filter)가 "이 파일이 경계를 넘어도 되는가"를 판정하는 데 쓰는 집행용 토큰이다. (N2SF-IF-7 데이터 유형 식별자 통제, N2SF-IF-14 보안등급 기반 흐름 통제)
- **티켓 개념**: 단일 조직 안에서 라벨은 탑승권과 같다. 게이트가 라벨을 확인하고 O등급만 통과시킨다. 라벨 없는 파일(U)은 기본 차단(default deny).
- **서명이 라벨을 보증한다**: 라벨 필드 + 콘텐츠 해시를 묶어 기관 키로 서명한다. 라벨만 서명하면 "S문서에 O라벨 붙여넣기"를 못 잡고, 해시 없이 서명하면 "O라벨 껍데기에 S내용 바꿔치기"를 못 잡는다. 그래서 반드시 **라벨+해시 결합 서명**.
- **여권 개념**: 발급 조직과 검증 조직이 다를 때, 라벨은 여권이 된다. 검증 측은 (a) 발급기관 인증서가 국가 루트 CA 체인으로 이어지는지, (b) 발급기관과 **등급 등가 협약**이 체결되어 있는지를 확인한다. 등급 체계는 기관마다 달라도 되고, 통일이 아니라 **등가 매핑**으로 상호 인정한다. (미국 CUI 레지스트리, NATO 등급 등가표와 같은 원리)
- **라벨 제거는 추적성을 지우지 못한다**: 라벨이 박리되어도 콘텐츠 해시는 파일과 불가분이므로, 인스턴스 레지스트리(해시→라벨 이력) 조회로 원 분류를 복원·귀속할 수 있다. 단 정책상 라벨 무효 파일은 U로 취급하여 차단한다.
- **분류 근거가 기관 간 번역의 열쇠**: 등급 값(S)은 기관마다 의미가 다르지만, "정보공개법 제9조 제6호(개인정보) 사유의 S"라는 근거는 어느 기관이 받아도 동일하게 해석된다.

---

## 2. 데이터 모델

### 2.1 가상 조직 (4개, 페이지 로드 시 키쌍 실제 생성)

| id | 명칭 | 역할 | 신뢰 상태 |
|---|---|---|---|
| `root` | 국가 루트 CA (국정원·KISA 역할) | 루트 인증기관. 기관 인증서 발급 | 신뢰 앵커 |
| `kpost` | 우정사업본부 | 주 시연 조직. 라벨 발급 + 게이트 운영 | 루트 체인 ○ / 협약 ○ |
| `ndata` | 국가데이터청 (가상) | 여권 시나리오의 상대 기관 | 루트 체인 ○ / 협약 ○ |
| `kari` | 한국행정연구원 (가상) | 거부 시나리오용 | 루트 체인 ○ / **협약 ×** |

- 페이지 로드 시 `crypto.subtle.generateKey`로 조직마다 ECDSA P-256 키쌍을 실제 생성한다.
- "인증서"는 X.509 전체를 구현하지 말고 **간이 인증서 객체**로 구현한다: `{ subject, publicKeyJwk, issuer, signature }` — issuer(root)의 개인키로 `subject+publicKeyJwk` 직렬화 바이트를 서명. 체인 검증 = root 공개키로 이 서명을 실제 검증.
- 화면에는 각 조직 카드에 공개키 지문(SHA-256 앞 16 hex)을 표시해 "각자 다른 키"임을 보이게 한다.

### 2.2 등급 등가 협약 테이블 (여권 스테이지의 핵심 데이터)

기관마다 등급 명칭이 다르다는 설정을 일부러 넣는다 (통일이 아닌 등가 매핑임을 보여주기 위해):

```js
const EQUIVALENCE = {
  // 우정사업본부가 인정하는 타 기관 등급 → 자기 등급 번역
  kpost: {
    ndata: { "공개": "O", "민감": "S", "기밀": "C" },   // 국가데이터청은 한글 등급명 사용
    // kari 항목 없음 = 협약 미체결 → 등가 판정 불가 → 거부
  }
};
```

### 2.3 분류기준 레지스트리 (criteria registry)

서명된 정적 데이터로 포함. 8~10개 항목이면 충분:

```js
const CRITERIA = [
  { id:"PRIV",  basis:"정보공개법 §9 제6호", desc:"개인정보(성명·주민번호 등)", grade:"S", controls:["N2SF-IF-14","N2SF-DU-2"] },
  { id:"BIZ",   basis:"정보공개법 §9 제7호", desc:"법인 영업상 비밀", grade:"S", controls:["N2SF-IF-14","N2SF-DA-4"] },
  { id:"AUDIT", basis:"정보공개법 §9 제5호", desc:"감사·내부검토 정보", grade:"S", controls:["N2SF-IF-14"] },
  { id:"LAW",   basis:"정보공개법 §9 제1호", desc:"법률상 비밀·비공개", grade:"C", controls:["N2SF-IF-14","N2SF-EK-1"] },
  { id:"OPEN",  basis:"해당 없음",           desc:"공개 정보", grade:"O", controls:[] },
  // ... 필요 시 추가
];
```
- 이 배열의 직렬화 바이트를 root 키로 서명해 두고, 에이전트가 기동 시 1회 검증한다. 검증 성공 시 레지스트리 패널에 "기준 레지스트리 서명 유효 ✓ (국가 루트)" 배지 표시. **기준마저 위변조 통제 대상**이라는 메시지 전달용.

### 2.4 인스턴스 레지스트리 (해시 → 라벨 이력)

- 화면에 항상 보이는 append-only 테이블. 라벨 발급 시마다 한 행 추가:
  `{ hash(16 hex 축약, hover 시 전체), 파일명, 등급, 근거, 발급조직, 승인자, actionId, 시각 }`
- 행 삭제·수정 UI 없음 (append-only임을 강조). 새 행 추가 시 하이라이트 애니메이션.
- 조직별로 소유 표시(발급조직 컬럼) — "인스턴스 레지스트리는 기관별 분산" 개념 반영.

### 2.5 가상 파일

각 파일은 다음 구조:

```js
{
  id, name,                    // 예: "2026_회의자료.hwp"
  format: "hwp"|"hwpx"|"docx"|"pdf",
  org: "kpost",                // 소속(발급 가능 조직)
  contentText: "...",          // 본문 텍스트 (편집 가능)
  contentBytes: Uint8Array,    // contentText의 UTF-8 인코딩 (해시 대상)
  label: null | {              // 라벨 객체 (없으면 null = 미분류 U)
    grade: "O"|"S"|"C",
    gradeDisplay: "O",         // 발급 기관의 표기 (ndata면 "공개" 등)
    basisId: "OPEN",           // criteria 참조
    issuerOrg: "kpost",
    approver: "오주상 계장",    // O등급 인간 승인자 (시연용 가상 표기 가능)
    actionId: "ACT-2026-....", // 발급 시 생성
    issuedAt: ISO문자열,
    contentHash: hex,          // 발급 시점 콘텐츠 SHA-256
    signature: base64          // sign( canonical(label필드들) + contentHash )
  }
}
```

**초기 샘플 파일 세트 (라벨 유무·포맷·조직을 교차 배치):**

| 파일명 | 포맷 | 조직 | 초기 상태 | 용도 |
|---|---|---|---|---|
| `보도자료_우체국금융.hwp` | hwp | kpost | 라벨 없음(U) | 발급 과정 시연의 시작점 |
| `2026_사업계획_초안.hwpx` | hwpx | kpost | S 라벨 (서명 유효) | S 차단 시연 |
| `직원_연락망.docx` | docx | kpost | S 라벨 (서명 유효) | 위조 시연 대상 |
| `공개_통계요약.pdf` | pdf | kpost | O 라벨 (서명 유효) | 정상 통과 시연 |
| `데이터개방_협조요청.hwpx` | hwpx | **ndata** | "공개" 라벨 (ndata 서명) | 여권 정상 인정 |
| `연구용역_중간보고.pdf` | pdf | **kari** | 라벨 있음 (kari 서명) | 여권 거부 (협약 미체결) |

- 본문 텍스트에 실감을 준다. S 파일에는 가상의 개인정보 패턴(예: `900101-1******` 식 마스킹된 주민번호 형태), O 파일에는 일반 공개 문구.
- 콘텐츠는 사용자가 텍스트영역에서 직접 편집 가능해야 한다 (내용 치환 시연용).

### 2.6 라벨 서명의 정규화(canonicalization)

서명 대상 바이트 = `JSON.stringify`를 **키 정렬 고정 순서**로 직렬화한 문자열의 UTF-8:
`grade|basisId|issuerOrg|approver|actionId|issuedAt|contentHash` 순서의 고정 배열을 join("\n") 하는 방식도 허용. 발급과 검증이 동일 함수를 쓰도록 `canonicalizeLabel(label)` 단일 함수로 구현한다.

---

## 3. 화면 구성

### 3.1 전체 레이아웃 (데스크톱 우선, 1280px 기준)

```
┌────────────────────────────────────────────────────────────────┐
│ 헤더: Signet — N2SF 문서 라벨 시연 | [Stage 1 티켓] [Stage 2 여권] 탭 │
├────────────────────────────────────────────────────────────────┤
│ 개념 해설 밴드 (접기 가능): 현재 스테이지의 원리 도해 + 3문장 해설 │
├──────────────┬──────────────────────────┬──────────────────────┤
│ ① 조직/파일   │ ② 파일 X-ray + 라벨 뷰    │ ③ 폴더 이동 & 에이전트 │
│  패널        │   (선택 파일의 내부 구조)   │   관문 (내부 폴더 →    │
│  (내부 폴더)  │   + 조작(tamper) 버튼      │   콘솔 → 외부 폴더)    │
├──────────────┴──────────────────────────┴──────────────────────┤
│ ④ 레지스트리 뷰어 (탭: 인스턴스 레지스트리 / 분류기준 레지스트리)  │
├────────────────────────────────────────────────────────────────┤
│ ⑤ 시나리오 바: [시나리오 1~6 버튼] — 클릭 시 자동 시연 진행       │
└────────────────────────────────────────────────────────────────┘
```

모바일 대응은 요구하지 않는다 (시연은 발표용 노트북/빔). 다만 1024px까지는 깨지지 않게.

### 3.2 ① 조직/파일 패널

- 상단: 조직 카드 4장 (국가 루트 CA는 작게, 앵커 표시). 각 카드: 조직명, 역할 한 줄, 키 지문, 협약 배지("우정사업본부와 등가 협약 ✓/✗").
- 하단: 파일 목록. **각 파일 카드에 라벨 상태를 즉시 식별 가능한 스탬프로 표시** (아래 3.5 라벨 시각 언어 참조).
- 파일 클릭 → ② 패널에 로드.

### 3.3 ② 파일 X-ray 뷰 — 이 데모의 시각적 심장

선택된 파일의 **포맷별 내부 구조**를 트리/블록으로 렌더링한다. 실물 파서가 아니라 포맷 구조의 충실한 도해다:

- **HWP (5.x)**: "CFB 복합문서" 구조 트리
  ```
  📦 보도자료_우체국금융.hwp  (CFB 복합문서)
  ├─ FileHeader
  ├─ DocInfo
  ├─ BodyText/Section0        ← [본문] 클릭 시 편집 가능
  ├─ PrvText
  └─ 🏷 N2SFLabel  ← 커스텀 스트림 (라벨 있을 때만 존재, 강조 표시)
  ```
- **HWPX / DOCX**: "ZIP + XML" 엔트리 목록
  ```
  📦 2026_사업계획_초안.hwpx  (OWPML: ZIP+XML)
  ├─ mimetype
  ├─ Contents/section0.xml    ← [본문]
  ├─ META-INF/manifest.xml
  └─ 🏷 META-INF/n2sf-label.xml  ← 커스텀 파트
  ```
  DOCX는 `word/document.xml`, `🏷 customXml/n2sfLabel.xml` 등으로 대응.
- **PDF**: 오브젝트 블록 나열 + `🏷 XMP 메타데이터 (n2sf: 네임스페이스)` 블록.

**라벨 노드 상세**: 🏷 노드를 클릭하면 라벨 카드가 펼쳐진다 — 등급 대형 배지, 근거(제9조 몇 호), 발급조직 + 직인 아이콘, 승인자, ActionId, 발급시각, contentHash(축약), 서명값(축약), 그리고 **실시간 서명 상태 인디케이터** (✓ 유효 / ✗ 불일치 / — 없음). 이 인디케이터는 표시 시점에 실제 `verify()`를 돌린 결과다.

**조작(tamper) 컨트롤** — X-ray 뷰 하단에 3개 버튼:
1. `등급 조작 (S→O 바꿔치기)`: 라벨의 grade 필드만 O로 변경 (서명은 그대로 둠) → 라벨 카드의 서명 인디케이터가 즉시 ✗로 바뀌는 것이 보여야 함.
2. `본문 변조 (개인정보 삽입)`: contentText에 주민번호 패턴 한 줄 삽입 → contentBytes 재인코딩 → 해시 불일치 유발. 삽입된 줄은 본문 미리보기에서 빨간 하이라이트.
3. `라벨 박리 (스트림 삭제)`: 🏷 노드가 애니메이션으로 뜯겨나가며 트리에서 제거, label=null이 아니라 **`stripped:true` + 원 라벨 백업 보관** (레지스트리 폴백 시연은 해시로 하므로 파일 객체상 label=null 처리, 레지스트리에는 이력이 남아 있음).

조작 후 파일 카드의 스탬프도 즉시 갱신된다.

### 3.4 ③ 폴더 & 에이전트 콘솔 (전송 = 폴더 간 이동 모델)

파일 전송은 **폴더 간 이동**으로 표현한다. 단, 이동 경로 한가운데에 에이전트 콘솔(관문)을 물리적으로 배치해 "이동은 반드시 심사를 거친다"는 구조가 시각적으로 유지되어야 한다:

```
┌─ 📁 업무망 (우정사업본부 내부) ─┐   ┌─ 에이전트 관문 ─┐   ┌─ 📁 외부 반출함 ─┐
│  파일 카드들 (스탬프 포함)      │ → │  검증 파이프라인  │ → │  통과한 파일만    │
└──────────────────────────────┘   └────────────────┘   └────────────────┘
```

- 파일 선택 후 `[외부로 이동]` 버튼 (또는 외부 폴더로 드래그) → 파일 아이콘이 관문 위치로 이동해 잠시 머무는 동안 콘솔에 파이프라인 로그가 진행 → **PASS면 파일이 외부 폴더에 실제로 옮겨져 남고**, BLOCK이면 원 폴더로 되돌아오며 파일 카드에 붉은 `차단됨 · 사유` 태그가 붙는다.
- 폴더는 상태를 지속한다: 시연이 진행될수록 외부 폴더에는 통과한 파일이, 내부 폴더에는 차단 태그 달린 파일이 누적되어 그 자체가 시연 이력판이 된다. `[처음으로 되돌리기]` 시 폴더 구성도 초기화.
- **여권 스테이지의 폴더 구성**: `📁 국가데이터청 반출함` / `📁 한국행정연구원 반출함` → 관문(검증 주체: 우정사업본부) → `📁 우정사업본부 수신함`. 발신 조직의 폴더에서 수신 조직 폴더로의 이동이 곧 국경 통과이며, 관문 패널에 "검증 주체: 우정사업본부" 라벨을 명시한다.
- 애니메이션 투자는 이 이동 한 곳에 집중한다 (파일 이동, 관문 체류, 통과/반송).

**에이전트 콘솔** — 관문 패널 내부에 판정 파이프라인을 단계별 로그로 출력. 각 단계는 실제 연산 완료 후 순차 표시(단계당 300~500ms 간격, 시연 리듬용):

```
[1/5] 라벨 추출 .............. 발견 (META-INF/n2sf-label.xml)
[2/5] 발급기관 체인 검증 ...... 우정사업본부 ← 국가 루트 CA ✓
[3/5] 서명·해시 검증 ......... 라벨 서명 ✓ / 콘텐츠 해시 일치 ✓
[4/5] 등급 판정 .............. O (근거: 해당 없음·공개) — 기준 레지스트리 대조 ✓
[5/5] 게이트 정책 ............ O등급 외부 반출 허용
▶ 판정: 통과 (PASS)
```

실패 시 실패 단계에서 빨간 로그 + 사유, 그리고 **폴백 단계**가 추가된다:

```
[3/5] 서명·해시 검증 ......... 서명 불일치 ✗ (라벨 필드가 서명 이후 변경됨)
[폴백] 인스턴스 레지스트리 조회 . 해시 a3f9… → 원 분류: S (§9 제6호, 2026-09-01, 승인 오주상)
▶ 판정: 차단 (BLOCK) — 라벨 위조 의심. 원 분류 S로 귀속됨
```

- 여권 스테이지에서는 [2/5]와 [4/5]가 확장된다: 체인 검증 + **협약 확인** + **등가 번역**("ndata '공개' → kpost 'O' (등가 협약 §3)"). kari 파일은 [협약 확인] 단계에서 실패: "체인은 유효하나 등가 협약 미체결 → 등급 해석 불가 → U 취급 차단".

### 3.5 라벨의 시각 언어 (가시성 요구의 구현)

라벨을 **직인(관인) 스탬프** 모티프로 통일한다 — 한국 행정문서의 시각 관습이라 평가위원에게 즉시 읽힌다:

- **O등급**: 원형 스탬프, 청록 계열, "O · 공개" — 통과 가능 상태.
- **S등급**: 원형 스탬프, 주황 계열, "S · 민감".
- **C등급**: 원형 스탬프, 적색 계열, "C · 기밀".
- **U(미분류/라벨 없음)**: 점선 원 + 회색 "라벨 없음" — 스탬프가 찍힐 자리가 비어 있는 형태로, "없음"을 적극적으로 시각화한다.
- **위조/불일치**: 스탬프 위에 사선 균열 오버레이 + "서명 불일치" 소형 경고 태그.
- **타 기관 라벨**: 스탬프 테두리를 이중선으로, 하단에 발급기관명 리본 (여권 스탬프 느낌).

이 스탬프는 파일 카드·X-ray 라벨 노드·게이트 판정 결과에서 동일하게 재사용한다 (한 번 배우면 어디서든 읽히도록).

### 3.6 개념 해설 밴드

- **Stage 1 (티켓)**: 소형 도해 — [문서+스탬프] → [게이트] → 통과/차단. 해설 3문장: "라벨은 문서가 경계를 넘어도 되는지 게이트가 판정하는 근거입니다. 라벨과 본문 해시를 묶어 기관 키로 서명하므로, 등급을 바꾸거나 본문을 바꾸면 검증이 깨집니다. 라벨이 없는 문서는 기본 차단됩니다."
- **Stage 2 (여권)**: 소형 도해 — [A기관 발급·서명] → [국가 루트 CA] ← [B기관 검증] + 등가표. 해설 3문장: "기관마다 등급 기준이 달라도 됩니다. 검증 기관은 국가 루트로 이어지는 서명 체인과 등가 협약을 확인해 상대 등급을 자기 등급으로 번역합니다. 협약이 없는 기관의 라벨은 체인이 유효해도 해석할 수 없어 차단됩니다."
- 각 밴드에 "미국 CUI·NATO 등급 등가표와 같은 원리" 각주 1줄.

### 3.7 ④ 레지스트리 뷰어 — "존재"뿐 아니라 "조회"를 시각화한다

두 레지스트리를 탭으로 표시하되, 레지스트리는 정적 표가 아니라 **에이전트가 실시간으로 참조하는 살아있는 원장**임을 보여야 한다.

**분류기준 레지스트리 탭:**
- 항목 테이블: `기준 ID | 근거 조항(§9 몇 호) | 설명 | 등급 | 필수 통제`. 등급 셀은 스탬프 소형 버전으로 렌더링.
- 상단에 "기준 레지스트리 서명 유효 ✓ (국가 루트 CA)" 배지 — 페이지 로드 시 실검증 결과. 배지 클릭 시 서명값·검증 시각 툴팁.
- **연동 하이라이트 ①**: 발급 폼에서 근거 드롭다운을 선택하면 이 탭의 해당 행이 하이라이트된다 (탭이 닫혀 있으면 탭 라벨에 점 표시) — "발급 폼의 선택지가 곧 레지스트리 항목"임을 보이는 장치.
- **연동 하이라이트 ②**: 파이프라인 [4/5] 등급 판정 시 대조된 행이 펄스 하이라이트.

**인스턴스 레지스트리 탭:**
- append-only 테이블 (2.4의 컬럼). 발급 시 새 행이 상단에 슬라이드 인 + 펄스.
- **폴백 조회의 극적 시각화**: 파이프라인의 [폴백] 단계가 실행되면 (a) 뷰어가 이 탭으로 자동 전환되고, (b) 콘솔의 해시값과 매칭된 행의 해시 셀이 동시에 하이라이트되며, (c) 매칭 행에 "← 조회됨" 임시 마커가 3초간 표시된다. 시나리오 4(라벨 박리)의 클라이맥스가 이 장면이다 — "라벨은 지워졌지만 레지스트리가 기억한다"가 텍스트가 아니라 화면 동작으로 전달되어야 한다.
- 조회했으나 이력이 없는 경우: 테이블 상단에 "해시 ○○… 이력 없음" 회색 배너 3초 표시.

**자동 탭 전환 규칙**: 에이전트가 어느 레지스트리를 참조하는 순간 해당 탭으로 전환한다. 시연자가 수동으로 탭을 조작한 직후 5초간은 자동 전환을 억제한다 (설명 도중 화면이 뺏기지 않도록).

---

## 4. 검증 파이프라인 (에이전트 로직 명세)

`verifyAtGate(file, verifierOrg)` — 반드시 아래 순서, 각 단계 결과를 콘솔 로그 배열로 반환:

1. **라벨 추출**: `file.label` 존재 확인. 없으면 → 5단계로 점프하되 폴백(4a) 먼저 수행.
2. **체인 검증**: 발급조직 인증서의 서명을 root 공개키로 `crypto.subtle.verify` 실검증.
3. **서명·해시 검증**: (a) `canonicalizeLabel` 재구성 바이트를 발급조직 공개키로 서명 검증, (b) 현재 `contentBytes`의 SHA-256을 실계산해 `label.contentHash`와 비교. 하나라도 실패 → 실패 사유 구분 출력(라벨 변조 vs 본문 변조) 후 4a 폴백.
4. **등급 판정**:
   - 동일 조직: 기준 레지스트리에서 basisId 조회, 등급·통제 표시.
   - 타 조직(여권): `EQUIVALENCE[verifierOrg][issuerOrg]` 존재 확인 → 없으면 "협약 미체결" 실패. 있으면 gradeDisplay를 자기 등급으로 번역해 표시.
   - **4a 폴백**: contentBytes 해시로 인스턴스 레지스트리 조회. 이력이 있으면 원 분류·승인자·시각을 귀속 표시(단, 판정은 U 유지). 없으면 "이력 없음".
5. **게이트 정책**: 최종 등급이 O(또는 O로 번역됨)이고 모든 검증 통과 → PASS. 그 외(S/C/U/검증실패) → BLOCK.

`issueLabel(file, grade, basisId, approver)` — 발급 로직: contentHash 실계산 → 라벨 구성 → canonical 서명 → 파일에 부착(X-ray 트리에 🏷 노드 등장 애니메이션) → 인스턴스 레지스트리 append → actionId 생성(`ACT-2026-` + 시각 기반).

발급 UI: U 파일 선택 시 X-ray 하단에 "라벨 발급" 폼 — 등급 선택, 근거 드롭다운(기준 레지스트리 항목), 승인자 입력(기본값 채움), [분류·서명·발급] 버튼. O등급 선택 시 "외부 공개 확정은 인간 승인 필요" 문구와 승인자 필드 강조 (인간 승인 원칙의 표현).

**분류 주체 원칙 (확정)**: 자동 분류는 이 데모에 존재하지 않는다. **등급은 시연자가 폼에서 직접 선택**하며, 이는 단순화가 아니라 "분류 기준의 수립과 확정은 기관(인간)의 책임"이라는 설계 사상의 표현이다. 폼 상단에 소형 안내 1줄: "등급 분류는 기관의 판단입니다 — 자동 분류 엔진은 제품화 확장 지점". 평가위원이 "자동 분류는?"이라고 물을 때의 답이 화면에 이미 있게 한다.

---

## 5. 시나리오 자동 시연 (시나리오 바)

각 버튼은 해당 상태를 준비하고 게이트 시도까지 자동 진행하되, 단계별로 사람이 따라올 수 있는 속도(총 15~25초)로 재생한다. 진행 중 관련 UI 요소를 포커스 하이라이트한다. `[처음으로 되돌리기]` 버튼으로 전체 상태 리셋(파일·레지스트리를 초기 세트로 복원 — 레지스트리는 초기 발급분만 남김).

**Stage 1 — 티켓:**
1. **정상 발급과 통과**: `보도자료.hwp`(U) 선택 → 외부 폴더로 이동 시도 → U 차단(내부 폴더로 반송) → 라벨 발급(시연자가 O 선택, 근거 OPEN, 승인자) → 레지스트리 행 추가 하이라이트 → 재이동 → PASS(외부 반출함에 안착). *메시지: 라벨 없으면 기본 차단, 발급 후 통과.*
2. **라벨 위조**: `직원_연락망.docx`(S) → [등급 조작 S→O] → 스탬프 균열 → 이동 시도 → [3/5] 서명 불일치 BLOCK·반송. *메시지: 서명이 라벨 바꿔치기를 잡는다 (MIP 무서명 한계의 봉합).*
3. **본문 변조**: `공개_통계요약.pdf`(O) → [본문 변조] → 이동 시도 → 해시 불일치 BLOCK·반송. *메시지: 라벨+해시 결합 서명이라 내용 치환도 잡힌다. 수정된 문서는 재분류 대상.*
4. **라벨 박리와 귀속**: `2026_사업계획_초안.hwpx`(S) → [라벨 박리] → 🏷 노드 뜯김 → 이동 시도 → 라벨 없음 → 폴백 레지스트리 조회 → "원 분류 S, §9 제5호, 승인자, 발급시각" 귀속 표시 → BLOCK·반송. *메시지: 라벨을 지워도 해시는 못 지운다 — 추적성은 레지스트리가 담보.*

**Stage 2 — 여권:**
5. **타 기관 라벨 인정**: `데이터개방_협조요청.hwpx`(국가데이터청 반출함, "공개" 라벨) → 우정사업본부 수신함으로 이동 시도 → 체인 ✓ → 협약 ✓ → 등가 번역("공개"→O) → PASS(수신함 안착). *메시지: 등급 통일 없이 등가 협약으로 상호 인정 — 여권과 같다.*
6. **협약 미체결 거부**: `연구용역_중간보고.pdf`(한국행정연구원 반출함) → 수신함 이동 시도 → 체인 ✓이지만 협약 ✗ → 등급 해석 불가 → U 취급 BLOCK·반송. *메시지: 서명이 진짜여도 등가 협약 없이는 등급을 번역할 수 없다. 상호 인정은 기술 반, 거버넌스 반.*

각 시나리오 종료 시 콘솔 하단에 1문장 결론 카드("이 시연이 증명한 것: …")를 띄운다.

---

## 6. 디자인 지침

**방향**: 정부 과제 평가장에서 신뢰감을 주는 절제된 관제 콘솔 + 한국 행정문서의 직인 모티프. 화려함보다 판독성. 대담함은 **직인 스탬프와 폴더 간 이동(관문 통과/반송) 애니메이션 두 곳에만** 쓴다.

- **팔레트** (제안, 구현 시 미세조정 가능):
  - 바탕 `#F7F8F6` (한지 느낌의 아주 옅은 회백), 패널 `#FFFFFF`, 잉크 `#1F2A33`
  - 등급색: O `#0E7C66`(관인 청록), S `#C97B1B`(호박), C `#B3362B`(주인朱印 적), U `#8A9199`(회색)
  - 게이트 PASS `#0E7C66` / BLOCK `#B3362B` — 등급색과 계열 공유
- **타이포**: 시스템 한글 스택 단일 사용 — `"Pretendard Variable", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif` (CDN 금지이므로 미설치 환경은 맑은고딕 폴백). 해시·키 지문·actionId 등 데이터 값만 `ui-monospace, Consolas` 모노스페이스.
- 코너 라운드는 위계 구분: 패널 8px, 스탬프는 정원, 버튼 4px. 그림자 남발 금지 — 게이트 판정 순간과 스탬프에만.
- 애니메이션은 사용자 행위에 대한 응답만: 게이트 통과/차단, 🏷 노드 등장/뜯김, 레지스트리 행 추가, 스탬프 균열. 페이지 로드 시 장식성 모션 금지. `prefers-reduced-motion` 존중.
- 콘솔 로그는 밝은 배경 위 어두운 텍스트로 (검은 터미널 스타일 금지 — 빔프로젝터 가독성).

---

## 7. 구현 순서 제안 (Claude Code 작업 단계)

1. 골격: 레이아웃 + 탭 + 상태 저장소(단일 전역 `state` 객체) + 리셋.
2. 암호 계층: 키 생성, 간이 인증서 발급/검증, `canonicalizeLabel`, sign/verify, sha256 유틸. **이 계층을 먼저 콘솔 테스트로 검증** (정상 서명 verify=true, 필드 1개 변경 시 false).
3. 데이터 초기화: 조직 4, 기준 레지스트리(+서명), 샘플 파일 6, 초기 라벨 발급(로드 시 실서명으로 생성 — 하드코딩 서명값 금지, 매 로드마다 새 키로 새로 서명).
4. 파일 X-ray 뷰 + 라벨 카드 + 스탬프 컴포넌트.
5. 발급 폼 + 조작 버튼 3종.
6. 게이트 비주얼 + 에이전트 콘솔 + `verifyAtGate`.
7. 여권 스테이지 (등가표, 확장 파이프라인).
8. 시나리오 자동 재생 6종 + 결론 카드.
9. 해설 밴드 + 각주 + 마감 점검.

## 8. 수용 기준 체크리스트

- [ ] `index.html` 단일 파일, 외부 요청 0건 (Network 탭 확인), file://에서 동작
- [ ] 페이지를 새로고침하면 키·서명이 새로 생성되고 모든 초기 라벨의 서명 검증이 통과
- [ ] 등급 조작 → 서명 인디케이터 즉시 ✗, 게이트 BLOCK (실검증 결과)
- [ ] 본문 1글자 수정만으로도 해시 불일치 BLOCK
- [ ] 라벨 박리 후 게이트에서 레지스트리 폴백으로 원 분류·승인자·시각이 표시되고 판정은 BLOCK
- [ ] ndata 파일이 "공개"→O 번역과 함께 PASS, kari 파일이 "협약 미체결" 사유로 BLOCK
- [ ] 파일 카드만 보고 라벨 유/무/등급/위조 상태가 구별됨 (스탬프 시각 언어)
- [ ] HWP는 CFB 스트림 트리, HWPX/DOCX는 ZIP 엔트리, PDF는 XMP 블록으로 X-ray가 서로 다르게 렌더링됨
- [ ] 시나리오 6종이 각각 클릭 한 번으로 끝까지 자동 재생되고 리셋이 완전함
- [ ] 인스턴스 레지스트리가 append-only로 동작하고 발급 시 실시간 반영
- [ ] PASS 시 파일이 외부/수신 폴더로 실제로 이동해 남고, BLOCK 시 원 폴더로 반송되며 차단 사유 태그가 유지됨 (폴더 = 시연 이력판)
- [ ] 자동 분류 요소가 UI 어디에도 없고, 발급 폼에 "등급 분류는 기관의 판단" 안내가 표시됨
- [ ] 폴백 조회 시 인스턴스 레지스트리 탭이 자동 전환되고 매칭 행이 하이라이트됨; 파이프라인 [4/5] 시 기준 레지스트리 해당 행이 하이라이트됨 (조회의 시각화)

## 9. 비범위 (구현 금지 — 질문 시 "제품화 확장 지점"으로 답변)

- 실물 파일 업로드/다운로드/파싱, 실제 HWP 바이너리 생성
- 서버, DB, REST API, 계정/로그인, 관리 화면
- 자동 분류·AI 분류 일체 (등급은 시연자 수동 선택 — 발급 폼의 등급·근거 선택으로 대체)
- X.509 실규격, CRL/OCSP, 타임스탬프
- 모바일 대응, 다국어

## 10. 발표자용 한 줄 서사 (페이지 푸터에 소형 표기)

"라벨은 스스로 문서를 지키지 않는다 — 인장이 라벨을 보증하고, 관문이 검증하며, 원장이 기억한다. 조직 안에서는 티켓, 조직 사이에서는 여권. **Signet.**"

## 11. UI 내 모듈 명칭 매핑

패널 제목에 모듈 브랜드를 병기해 제품 구조가 화면에서 읽히게 한다 (과하지 않게, 패널 제목에만):
- 발급 폼 영역: `Signet Label — 라벨 발급`
- 에이전트 관문 패널: `Signet Gate — 검증 관문`
- 레지스트리 뷰어: `Signet Ledger — 레지스트리`
- 여권 스테이지 해설 밴드: `Signet Federation — 기관 간 상호 인정` 부제
- 그 외 본문·콘솔 로그에는 브랜드명을 반복하지 않는다 (로그는 기능 서술만).
