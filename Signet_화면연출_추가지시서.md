# Signet 데모 — 화면 연출 추가 지시서 (델타)

> 진행 중인 구현에 추가 적용할 변경분만 담은 문서. 기존 스펙(데이터 모델·검증 파이프라인·시나리오·레지스트리 시각화)은 그대로 유지한다.
> 적용 범위: ① 오프닝 시퀀스, ② 발급·차단 물리 연출, ③ 도전 모드, ④ 상용 제품 질감 계층, ⑤ 슬로건 제거.

---

## 0. 먼저 제거할 것 (기존 구현에 있다면)

- 오프닝·본 화면·푸터의 **모든 슬로건형 문장** 제거: "라벨은 스스로 문서를 지키지 않습니다", "인장이 보증합니다" 류의 타이포 훅, 푸터 한 줄 서사 포함. 화면에는 서술 문장이 아닌 시스템 로그·데이터·기능 라벨만 존재한다.
- 이모지 아이콘, 브라우저 기본 select/scrollbar 스타일, 균일 라운드 카드+동일 그림자 패턴이 있다면 §4의 자체 디자인 시스템으로 교체.

---

## 1. 콜드 오픈 — 무언의 부팅 시퀀스 (8~10초, 스킵 가능)

문장 없이 인장 애니메이션 + 실연산 로그만으로 구성. 어두운 무대(`#141A1F`)에서 시작:

- **0~2초**: Signet 로고 마크(인장 반지 아이콘) 중앙 점등. 하단 소형 표기 `Signet 1.4.2 · build 시작`.
- **2~5초**: 인장이 하강해 밀랍에 눌리고, 각인이 O등급 관인 스탬프로 모핑. (연출 품질을 여기에 집중)
- **2~8초 (병행)**: 스탬프 아래 실연산 로그가 터미널 상태 라인으로 흐름. 각 라인은 해당 실연산(generateKey/sign/verify) **완료 이벤트에 동기**해 출력:
  ```
  keygen  root-ca ......... ECDSA P-256  a3f9 8c21 ✓
  keygen  kpost ........... ECDSA P-256  7bd0 42ee ✓
  keygen  ndata ........... ECDSA P-256  91c4 0af7 ✓
  keygen  kari ............ ECDSA P-256  e28b 55d1 ✓
  sign    initial-labels .. 6/6 ✓   precomputed: none
  ledger  criteria-registry  root-ca signature ✓
  ```
  ("하드코딩 없음" 선언은 문장이 아니라 `precomputed: none` 로그 토막으로만.)
- **8~10초**: `boot complete` 후 밝은 본 화면으로 디졸브. 오프닝의 관인 스탬프가 첫 파일 카드의 스탬프 위치로 날아가 안착 (오프닝과 본 UI의 시각 언어 연속).

필수 동작:
- 클릭/ESC 즉시 스킵 (우하단 "건너뛰기" 상시 표시)
- 같은 세션 재로드 시 자동 스킵 (sessionStorage)
- `prefers-reduced-motion`: 로그만 정적 표시 후 즉시 진입
- 실연산이 빨리 끝나도 최소 리듬 유지, 늦으면 로그 단계에서 대기

---

## 2. 발급·차단의 물리 연출 (본 화면에서 허용되는 유이한 볼드 모먼트)

- **발급**: [분류·서명·발급] 클릭 → 관인 스탬프가 상단에서 하강해 파일 카드에 찍힘 — 2~3px 화면 진동 1회, 잉크 번짐→선명 0.4초. 동시에 Ledger에 행 슬라이드 인 ("찍는 순간 원장에 기록"이 한 호흡).
- **차단**: 파일이 관문에서 반송되며 스탬프 균열 클로즈업(카드 확대 0.3초) + 콘솔 실패 라인 타자기 출력. 연출 밀도는 시나리오 2(위조)·4(박리 귀속)에 집중.
- 진동·확대는 각 1회성, 연속 재생 금지. 사운드는 토글 제공하되 기본 OFF.

---

## 3. 심사위원 도전 모드

시나리오 바 끝에 `[도전: 직접 변조해 보십시오]` 버튼:
1. 안내 오버레이: "본문에 아무 문구나 입력해 보십시오. 1글자만 바뀌어도 관문이 잡아냅니다."
2. O등급 파일의 본문 편집창 포커스 → 입력 즉시 해시 프리뷰가 실시간 변경 표시
3. [이동 시도] → 해시 불일치 차단 + 레지스트리 폴백 귀속 표시
4. 종료 후 [원상 복구] 1클릭 제공

심사위원이 직접 만든 변조가 실검증으로 잡히는 것이 데모의 최종 증명이므로, 이 흐름은 클릭 3회 이내로 매끄러워야 한다.

---

## 4. 상용 제품 질감 계층 (전 항목 구현)

목표: "이미 운영 중인 제품의 한 화면" 인상. 큰 연출이 아니라 아래 디테일의 총합으로 만든다.

### 4.1 제품 크롬
- **상단 앱 바**: 좌측 Signet 로고 마크 + 제품 내비게이션 `대시보드 · 문서 · 관문 · 원장 · 연합 · 감사 · 설정`. 데모에서 동작하는 항목만 활성, 나머지는 비활성(회색, hover 시 "이 시연 범위 밖" 툴팁). 비활성 메뉴의 존재가 "더 큰 제품의 한 조각" 인상을 만든다.
- **앱 바 우측**: 조직 전환 드롭다운(현재: 우정사업본부 — 여권 스테이지의 검증 주체 전환과 연동), 사용자 아바타(이니셜 원형 "오주상 · 정보보안팀"), 알림 벨(뱃지 1~2, 클릭 시 최근 차단 이벤트 목록).
- **헤더 배지**: `시연 테넌트 (DEMO)` 소형 배지 — SaaS 환경 배지 관습 차용으로 리얼리티를 높이면서, 운영 지표가 시연 데이터임을 정직하게 표시.
- **하단 상태바**: `● 관문 연결됨 · 원장 동기화 14:02:31 · Signet 1.4.2 (build 8f3a91c)` — build 해시는 로드 시 실계산한 임의 해시 앞 7자리 (매 로드 변경).
- **파비콘**(인장 마크 SVG data-URI) + 탭 타이틀 `Signet — 문서 라벨 관리`.

### 4.2 운영의 흔적
- **지표 스트립**: `오늘 검증 1,247 · 통과 1,193 · 차단 54 · 등록 문서 8,412` — 시연 중 실제 판정 발생 시 해당 카운터 **실증가(+1)**. 정적 숫자에 실동작이 섞여야 숫자 전체가 살아 보인다.
- **초기 원장 채우기**: 인스턴스 레지스트리에 초기 이력 15~20행 (지난 2주 날짜 분포, 다양한 파일명·승인자). 초기 행도 로드 시 **실서명으로 생성** — 하드코딩 서명값 금지 원칙 유지.
- **파일 카드 실데이터 밀도**: 크기(KB), 최종 수정자, 수정 시각, 포맷 아이콘. 빈 필드·placeholder 금지.
- **감사 토스트**: 차단 발생 시 우하단 `차단 이벤트가 감사 로그에 기록되었습니다 · ACT-2026-…` 2.5초.

### 4.3 디자인 시스템 고유성 (AI 기본 프레임 티 제거)
- 아이콘: 이모지·기성 세트 금지. 단일 스트로크 스타일 자체 SVG 12~15종(문서, 인장, 관문, 원장, 조직, 열쇠, 협약 등) 인라인 정의, 전 화면 일관 사용.
- 카드 라운드·그림자·간격 차등 적용 (모든 요소가 같은 라운드/그림자 = AI 프레임 티). 패널 8px / 스탬프 정원 / 버튼 4px.
- 버튼·입력·드롭다운·토스트·툴팁·스크롤바를 자체 스타일로 통일 (특히 select).
- 표: 조밀한 행 높이(엔터프라이즈 데이터 밀도), hover 행 하이라이트, 고정 헤더.

### 4.4 정직성 가드
운영 지표·초기 이력은 DEMO 배지 하의 시연 데이터. **실제 고객·실적 주장 금지** — 고객사 로고, "도입 기관 N곳" 류 문구 넣지 않는다. 상용 질감은 화면 밀도로 만들고, 사실 주장은 하지 않는다.

---

## 5. 수용 기준 (이 델타분만)

- [ ] 오프닝이 실연산 완료 이벤트에 동기 재생되고, ESC/클릭 스킵·세션 자동 스킵 동작
- [ ] 오프닝·본 화면·푸터 어디에도 슬로건형 문장 없음 (로그·데이터·기능 라벨만)
- [ ] 발급 시 스탬프 압인(진동 1회+잉크 번짐), 차단 시 균열 클로즈업이 각 1회성으로 재생
- [ ] 도전 모드: 임의 입력 → 실검증 차단 → 원상 복구 1클릭
- [ ] 앱 바·비활성 내비·조직 드롭다운·상태바(실계산 빌드 해시)·파비콘·DEMO 배지 적용
- [ ] 지표 스트립이 실제 판정 시 실증가, 초기 원장 15행 이상 전부 실서명 생성
- [ ] 이모지·브라우저 기본 컨트롤 스타일이 화면에 없음
- [ ] `prefers-reduced-motion` 존중, 사운드 기본 OFF
