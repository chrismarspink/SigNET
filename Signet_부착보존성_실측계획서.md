# Signet 부착 보존성 실측 계획서

> **목적**: 라벨을 실제 문서 파일에 부착했을 때, 편집기가 그 파일을 열고 다시 저장해도 (a) 라벨이 살아남고 (b) 콘텐츠 해시가 여전히 일치하는지를 측정한다.
> **위치**: 데모(`index.html`)는 이 방식이 논리적으로 성립함을 보였다. 이 실측은 **실물 파일에서도 성립하는지**를 확인하는 첫 관문이며, 통과 여부에 따라 부착 전략이 결정된다.
> **도구**: `cli/signet.mjs` (본 계획서 전용으로 제작). 계획서의 모든 절차는 이 CLI 명령으로 기술된다.

---

## 0. 왜 이것이 첫 번째 실험인가

Signet 체계 전체가 **"라벨이 문서에 붙어 다닌다"** 는 가정 위에 있다. 이 가정이 깨지면 — 한/글이 알 수 없는 스트림을 재저장 때 버린다면 — 임베디드 부착 전략 자체를 폐기하고 사이드카(원장) 중심으로 재설계해야 한다. 서버·PKI·관문 연동에 자원을 넣기 전에 이 답을 먼저 받아야 하는 이유다.

두 번째 가정은 더 미묘하다. **실파일은 내용을 고치지 않아도 재저장만으로 바이트가 바뀐다** (압축 파라미터, 엔트리 순서, 타임스탬프). 데모는 본문이 깨끗한 문자열이라 이 문제를 만나지 않았다. 파일 전체를 해시하면 첫 저장에서 바로 깨지므로, **무엇을 해시할 것인가**를 실측으로 정해야 한다.

---

## 1. 가설

| ID | 가설 | 깨질 경우의 영향 |
|---|---|---|
| H-1 | ZIP 계열(HWPX·DOCX)에 추가한 사용자 정의 파트는 편집기 재저장 후에도 보존된다 | 임베디드 부착 불가 → 사이드카 전환 |
| H-2 | HWP(CFB)의 커스텀 스트림은 한/글 재저장 후에도 보존된다 | HWP는 사이드카 전용으로 확정 |
| H-3 | PDF의 XMP 메타데이터는 뷰어·편집기 재저장 후에도 보존된다 | PDF는 사이드카 전용 |
| H-4 | 본문 파트만 해시(`payload`)하면 재저장 후에도 해시가 일치한다 | 해시 대상 재정의 또는 서명 대상 축소 |
| H-5 | 본문 텍스트 해시(`text`)는 서식 변경에도 안정적이다 | 텍스트 정규화 규칙 재설계 |

**대조군**: 파일 전체 해시(`whole`)는 재저장만으로 깨질 것으로 예상한다. 예상대로 깨지는 것을 확인하는 것이 `payload`의 필요성을 증명한다.

---

## 2. 자동과 수동의 경계 (중요)

CLI는 실험의 **계측기**이지 실험 전체가 아니다. 편집기로 열어 저장하는 행위는 사람이 해야 한다.

| 단계 | 주체 | 수단 |
|---|---|---|
| 시료 생성 | CLI | `specimen` |
| 라벨 부착 | CLI | `attach` |
| 부착 직후 검증 | CLI | `verify` |
| **편집기로 열기 → 편집 → 저장** | **사람 (Windows)** | 한/글, MS Word, Acrobat |
| 재저장본 판정 | CLI | `roundtrip --before --after` |
| 결과 집계 | 사람 | §6 기록표 |

CLI를 macOS/Linux에서 돌려 시료를 만들고, 시료를 Windows 시험 PC로 옮겨 편집기 작업을 한 뒤, 결과 파일을 되가져와 판정한다. 편집기 자동화(COM/매크로)는 1차 범위 밖으로 둔다 — 수동 조작이 오히려 실사용 경로에 가깝다.

---

## 3. 측정 지표

각 시험마다 네 가지를 기록한다. `roundtrip` 명령이 앞의 세 가지를 자동 판정한다.

1. **라벨 생존** — 재저장본에서 라벨을 추출할 수 있는가 (`○ 살아남음` / `× 유실`)
2. **라벨 무결** — 추출된 라벨이 부착본과 바이트 동일한가, 서명이 재검증되는가
3. **해시 안정성** — `whole` / `payload` / `text` 각각이 재저장 전후로 같은가
4. **파일 유효성** — 재저장본이 편집기에서 정상적으로 열리는가 (사람이 눈으로 확인)

---

## 4. 시험 매트릭스

**포맷 × 편집기**

| 포맷 | 편집기 | 부착 방식 | 비고 |
|---|---|---|---|
| HWPX | 한/글 2022 이상 | 임베디드 `META-INF/signet-label.xml` | 최우선 |
| HWPX | 한/글 NEO, 웹한글 | 임베디드 | 버전 편차 확인 |
| DOCX | MS Word 2019/365 | 임베디드 `customXml/signetLabel.xml` | |
| DOCX | 한/글(호환 열기) | 임베디드 | 교차 편집기 |
| PDF | Acrobat Reader/Pro | 임베디드 XMP | |
| PDF | 한/글 PDF 저장, 크롬 인쇄 | 임베디드 XMP | 완전 재생성 경로 |
| HWP | 한/글 | **사이드카** | CFB 쓰기 미구현(R-1) |

**조작 시나리오** — 각 조합에 대해 순서대로 수행한다.

| ID | 조작 | 기대 |
|---|---|---|
| A. 무편집 저장 | 열고 곧바로 저장 | 라벨 생존 ○, `payload` 안정 |
| B. 서식 변경 | 글꼴·색만 변경 후 저장 | 라벨 생존 ○, `payload` 변경(정상), `text` 안정 |
| C. 본문 수정 | 문장 1개 수정 후 저장 | **해시 불일치가 탐지되어야 함** (재분류 대상) |
| D. 다른 이름 저장 | 다른 파일명으로 저장 | 라벨 생존 ○ |
| E. 포맷 변환 | HWPX→HWP, DOCX→PDF 등 | 라벨 유실 예상 → 사이드카·원장 폴백 필요성 확인 |

C는 "깨져야 정상"인 시험이다. 여기서 탐지가 안 되면 체계의 존재 이유가 없다.

---

## 5. 절차

```bash
cd cli

# 1) 키·인증서 (실측용 — 운영은 GPKI/HSM)
node signet.mjs keygen --keys work/keys

# 2) 시료 생성
node signet.mjs specimen --out work/specimen

# 3) 라벨 부착 + 부착 직후 검증
node signet.mjs attach work/specimen/S-01_사업계획_초안.hwpx \
  --grade S --basis AUDIT --approver "김보라 사무관" --handling NO_REDISTRIBUTION
node signet.mjs verify work/specimen/S-01_사업계획_초안.hwpx     # ▶ 유효 (VALID)

# 4) 원본 보관 후 Windows 시험 PC로 이송
cp work/specimen/S-01_사업계획_초안.hwpx work/before/

#    ── 사람: 한/글로 열기 → 시나리오 A~E 수행 → work/after/ 로 회수 ──

# 5) 판정
node signet.mjs roundtrip --before work/before/S-01_사업계획_초안.hwpx \
                          --after  work/after/S-01_A_무편집저장.hwpx
```

`roundtrip`은 종료코드로 통과(0)/실패(1)를 반환하므로 다수 조합을 스크립트로 일괄 판정할 수 있다.
해시 대상별 값만 따로 보려면 `node signet.mjs hash <파일>`.

---

## 6. 기록 양식

| 시료 | 포맷 | 편집기·버전 | 시나리오 | 라벨 생존 | 라벨 무결 | whole | payload | text | 파일 정상 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|
| S-01 | HWPX | 한/글 2022 (11.0.0.x) | A | | | | | | | |
| S-01 | HWPX | 한/글 2022 | B | | | | | | | |
| … | | | | | | | | | | |

---

## 7. 판정 기준 (포맷별 Go/No-go)

- **Go (임베디드 채택)**: 시나리오 A·B·D에서 라벨 생존 ○ **이고** 시나리오 A에서 `payload` 안정 ○
- **조건부 Go**: 라벨은 생존하나 `payload`가 불안정 → 해시 대상을 `text`로 낮추고 재측정. `text`도 불안정하면 해당 포맷은 No-go
- **No-go (사이드카 전환)**: 시나리오 A에서 라벨 유실. 해당 포맷은 파일 내부 부착을 포기하고 원장 조회(해시 기반)로만 운용

시나리오 C에서 변조가 탐지되지 않으면 포맷 무관 **설계 결함**으로 즉시 중단하고 해시 대상 정의를 재검토한다.

---

## 8. 예비 결과 (CLI 제작 중 확인된 것)

실물 편집기 시험 전에, CLI 자체 시험에서 이미 확정된 사실이다.

**F-1. 자기참조 — 파일 전체 해시는 임베디드 부착과 양립할 수 없다 (확정)**
라벨이 파일 안에 들어가는 순간 파일 바이트가 바뀌므로, 파일 전체를 해시한 값은 부착 즉시 자기 자신과 어긋난다. PDF 임베디드 부착에서 이 결함이 실제로 재현되었다. **해결**: 부착 대상을 해시 대상에서 제외한다. ZIP 계열은 본문 파트만 해시하므로 이미 무관하고, PDF는 증분 업데이트가 원본 뒤에만 덧붙는 성질을 이용해 **첫 `%%EOF`까지를 본문으로 정의**했다. 검증 측이 별도 정보 없이 같은 구간을 다시 계산할 수 있으므로 추가 서명 대상이 필요 없다.

**F-2. 재압축만으로 `whole` 해시는 붕괴하고 `payload`·`text`는 안정 (시뮬레이션)**
내용을 그대로 두고 ZIP을 다시 묶는 것만으로 전체 해시가 바뀌었다. 같은 조건에서 `payload`·`text`는 동일했다. 실제 편집기 재저장은 이보다 변형이 크므로 H-4는 아직 미확정이나, `whole`을 배제해야 한다는 점은 확정이다.

**F-3. v2 스키마에 해시 대상 선언이 없다 (스키마 보정 제안)**
`content.hash`가 "무엇을 해시했는지"를 담지 않아 검증 측이 재계산 대상을 알 수 없다. **해결**: 값 자체에 대상을 실었다 — `sha256/payload:<hex>`. 이 필드는 이미 정규화·서명 대상이므로 스키마나 정규화 규칙을 바꾸지 않고도 서명으로 보호된다. 데모와 CLI의 서명 호환성이 유지되며, `cli/test/run.mjs`의 적합성 시험이 두 구현의 정규화가 바이트 단위로 같음을 강제한다.

---

## 9. 위험과 한계

| ID | 내용 | 대응 |
|---|---|---|
| **R-1** | **HWP(CFB) 쓰기 미구현** — 커스텀 스트림 삽입에는 CFB 라이터가 필요하다. 가장 위험한 포맷인데 1차에서 임베디드 측정이 불가하다 | 1차는 사이드카로 측정. H-2 검증은 CFB 라이터 확보(라이브러리 선정 또는 구현) 후 2차로 이월 |
| R-2 | PDF는 고전 xref만 지원 — xref 스트림(1.5+) 문서는 부착 거부 | 시료는 고전 xref로 생성. 실무 PDF 대응은 2차 |
| R-3 | 시료가 최소 골격이라 실제 업무 문서보다 단순 | 1차 통과 후 실제 업무 문서(개인정보 제거본)로 2차 재측정 |
| R-4 | 편집기 버전 편차 | 기관 표준 버전 우선, 결과표에 버전 명기 |
| R-5 | 시험용 키가 평문 JSON | 실측 전용. 운영은 GPKI 인증서 + HSM |

---

## 10. 일정과 산출물

| 단계 | 기간 | 산출물 |
|---|---|---|
| 1. 시료 생성·부착 | 0.5일 | `work/before/` 시료 세트 |
| 2. 편집기 시험 (수동) | 2일 | `work/after/` 재저장본 |
| 3. 판정·집계 | 0.5일 | §6 기록표 |
| 4. 결론 | 0.5일 | 포맷별 Go/No-go, 부착 전략 확정서 |

**이 실측이 끝나야 결정되는 것**: 포맷별 부착 방식(임베디드/사이드카), 해시 대상(payload/text), 그리고 HWP를 어떻게 다룰 것인가. 이 세 가지가 정해지기 전에는 서버·PKI·관문 연동 설계를 시작하지 않는다.
